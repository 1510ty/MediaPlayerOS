<UserControl x:Class="MediaPlayerOS_Csharp_WPF_Test_Edition.TestWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="300" Height="200"
             MouseMove="TestWindow_MouseMove"
             MouseLeftButtonUp="TestWindow_MouseLeftButtonUp">

    <Grid Background="#F0F0F0" MouseLeftButtonDown="TestWindow_MouseLeftButtonDown">
        <!-- タイトルバー -->
        <Grid Background="#0078D7" Height="30" VerticalAlignment="Top"
              MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <TextBlock Text="疑似ウィンドウ" Foreground="White" Margin="10,0"
                       VerticalAlignment="Center" FontWeight="Bold"/>
            <Button Content="✕" Width="30" Height="30" HorizontalAlignment="Right"
                    Click="CloseButton_Click" Background="Transparent" Foreground="White"
                    BorderThickness="0" FontSize="14" FontWeight="Bold" />
        </Grid>

        <!-- 内容 -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,30,0,30">
            <TextBlock Text="ここに内容を表示" Margin="10" />
        </StackPanel>

        <!-- サイズ変更ハンドル -->
        <Rectangle x:Name="ResizeGrip" Width="16" Height="16" Fill="Gray"
                   HorizontalAlignment="Right" VerticalAlignment="Bottom"
                   Cursor="SizeNWSE"
                   MouseLeftButtonDown="ResizeGrip_MouseLeftButtonDown"
                   MouseMove="ResizeGrip_MouseMove"
                   MouseLeftButtonUp="ResizeGrip_MouseLeftButtonUp"/>
    </Grid>
</UserControl>
